<h1 class="text-3xl font-bold underline font-serif">Articles</h1>


<%= link_to "Create new Article", new_article_path %>

<div class="row">
  <% @articles.each do |article| %>
    <div class="col-span-6 bg-slate-500">
      <h3 class="text-xl font-bold"><%= link_to article.title, article %></h3>
    </div>
  <% end %>
</div>
